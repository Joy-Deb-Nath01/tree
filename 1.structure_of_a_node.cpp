struct Node {
    int data;       // It will store the value of the node
    Node* left;     // It will store the address of the left child
    Node* right;    // It will store the address of the right child
};
/*
     10
   /  \
  5    20   //!now try to create a node like this

*/